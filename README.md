## Usage
````javascript
   $('#metro_moscow').metro({
          'placeholder': 'Выберите метро',
          'callback': (selected_metro) => {
              console.log(selected_metro)
           }
    });
 ````
